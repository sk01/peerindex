Gem::Specification.new do |s|
  s.name    = %q{peerindex}
  s.version = "1.0.0"  
  s.authors = ["Michael Fox"]
  s.email   = ["mike@mikefox.info"]
  s.summary = %q{Provides an interface to the PeerIndex REST API.}
  s.files = [
    "lib/peerindex.rb"
  ]
  s.require_paths = ["lib"]
end
